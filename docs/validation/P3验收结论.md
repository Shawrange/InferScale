# P3 验收结论

日期：2026-09-23。**按秋招项目范围，P3 验收通过。** GPU 取消采用用户本轮明确的人工确认，不再要求补充自动化取消证据；正式性能收益仍由 P4 实验回答。

本次新版 `inferscale-p3-final.tar.gz` 的 SHA-256 为 `d630a7cec3577e7dbb7742a5a583e4470b4352195d731a3dd1ad820cf3233d31`，不同于上次审核包。原始文件与校验结果已独立归档：[audit.json](server-20260923-accepted/audit.json)。这是提交材料的离线核对，没有远程重跑。

| 验收项 | 结论与依据 |
|---|---|
| 双真实副本 | 已有双 RTX 4090、Qwen2.5-7B-Instruct、vLLM 0.23.0、两个 TP=1 进程的部署证据 |
| 四策略协议 | 四份正式 JSON 各 12 项，合计 48 项接口组合完整，usage 算术正确；每轮 Gateway 完成 4 次，活动 request/attempt 归零，无失败/清理错误/超时 |
| Least Load | 新日志 4 条路由均为 least_load；串行空闲负载下 score=0 符合公式 |
| Cost | 新日志 4 条路由均为 cost；score 为 0.004/0.0069，与预占成本 40/69、reference=10000 吻合 |
| Prefix | 新日志 4 条路由均为 prefix；最后一次 affinity=1、score=-0.2431，等于 0.0069-0.25，亲和分已生效 |
| GPU 取消 | **用户人工确认通过**；保留网关 cancelled 日志及后端空闲快照。不将旧 JSON 的 false 改成 true，也不宣称归档独立证明了后端 abort |
| 缓存观测 | 已有 enable_prefix_caching=True 的有效配置及按 token 计的查询/命中指标；缓存收益不在本次功能验收中推定 |

采用新的 `gpu-cost-smoke.json`，保留但不计入旧备份 `gpu-cost-smoke.json~`。策略以实际 routing_decision 为准，包内单份 server.yaml 仍是 RR，不能用于描述其余各轮配置。本轮三份日志的 request_id 均能对应各自 completed 终态。

## 下一步：P4

1. 实现基准客户端和报告：固定到达轨迹、request/attempt 分表、失败/拒绝/取消完整分母、窗口与 drain 分开。
2. CPU 上验证 B1–B6 统计口径，再交付可直接上传服务器的实验包。
3. GPU 上运行 Mixed、Shared Prefix、Hot Prefix 三类负载，每组至少三轮；固定配置、预热/缓存条件，保留原始数据。
4. 生成 TTFT/E2E、吞吐与错误率对比，形成 README、演示与简历材料。当前 smoke 耗时和累计缓存快照不充当策略收益。

本轮只更新验收记录和规划，未修改应用代码；不重复执行此前已通过的 CPU 回归。
