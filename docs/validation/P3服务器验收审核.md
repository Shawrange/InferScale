# P3 服务器验收包审核

审核日期：2026-09-23。结论：**真实双副本的协议/usage 冒烟通过；取消只有手动验证声明，尚未形成可复核证据；正式性能实验未开展。**

输入为用户提交的 `inferscale-p3-validation.tar.gz`，SHA-256 为 `b6c3a0797fb7be3396c0bb0032033864adda17113727ebe73854eb6afb75ebc6`。本次为离线核对，未连接服务器重跑。原始 11 个文件保存在 [server-20260923](server-20260923/gpu-smoke.json)，逐文件摘要和机器核对结果见 [audit.json](server-20260923/audit.json)。

## 已确认范围

| 项目 | 提交证据与结论 |
|---|---|
| 部署形态 | AutoDL，无 Docker；两个原生 vLLM serve 进程，端口 8100/8101，均为 TP=1；两卡各有一个 EngineCore PID，与进程记录对应 |
| 硬件 | 两张 RTX 4090，单卡显示 24564 MiB；显存驻留不等于请求未取消 |
| 后端 | 报告 vLLM 0.23.0、Torch 2.11.0+cu130、Transformers 4.57.6、Python 3.12.3；驱动 595.58.03。nvidia-smi 的 CUDA 13.2 与 Torch CUDA 13.0 分别记录，不混写 |
| 模型 | Qwen/Qwen2.5-7B-Instruct；声明 revision `a09a35458c702b33eeacc393d103063234e8bc28`；BF16、context 2048；文件配置与模型 endpoint 一致。revision 为提交的配置声明，没有逐权重文件校验 |
| 生成协议 | 2 个后端 + Gateway × chat/completion × stream/non-stream，共 12 条且矩阵完整；报告标记协议通过 |
| usage | 12 条均满足 prompt + completion = total，输出不超过 smoke 的 32 token 上限；completion prompt=8、chat prompt=37，在两副本及网关间一致 |
| tokenizer | 单独提交的两份 tokenize JSON 相同；完整 smoke 声明已核对两个 API 的 token IDs。未保存全部原始 SSE/模板渲染结果，本次不能独立重放其终止事件 |
| 网关收尾 | 4 次 Gateway 成功；ready_workers=2，active_requests=active_attempts=0；retry/partial/cleanup failure/timeout/reject/failure 均为 0 |
| 路由范围 | 本轮配置是 round_robin；不等于其余三个策略已在 GPU 上分别验证 |

12 次耗时不能用于策略比较或代理开销结论。端口 8101 第一次 completion 非流式为约 1.058 秒，其他样例更短；缺少预热与重复样本，无法确定差异原因，不据此判为代码回归。

## 尚需补齐的证据

1. **GPU 取消**：NOTES 写“manually verified”，但 JSON `gpu_cancel_verified=false`、网关 cancelled=0，包内没有取消请求过程、服务日志或 vLLM 指标序列。两种记录反映的范围不同，先保留“人工声明，未验收”。补一次生成中途取消，留存请求 ID、取消前正在执行、取消后的网关终态/归零，以及后端停止的证据；不能仅凭最终空闲排除自然生成完毕。
2. **缓存与实际引擎参数**：NOTES 声明 metrics 可用，但未附原始 metrics。启动命令未显式指定 prefix caching 和 generation-config，不能据此断言启用或关闭；保存实际生效配置、运行日志和缓存指标名/单位，再做 Prefix 对比。
3. **复现实验版本**：这是原生进程部署，不必补不存在的镜像 digest；改为保存 vLLM/Gateway 各自依赖清单、Gateway 源码或包校验值、模型 snapshot 身份及启动配置。当前包没有 Gateway 源码摘要，无法独立确认远端代码完全等同本地打包版本。

## 下一步规划

- P3 收尾：补取消证据；保存有效后端配置和指标快照；Least Load、Cost、Prefix 分别做功能 smoke。
- 可并行准备 P4：实现固定到达轨迹、request/attempt 原始样本和分母校验的 runner/report。
- 正式 GPU 实验：统一预热、采样参数、缓存状态和模型版本后，运行 Mixed / Shared Prefix / Hot Prefix，每组至少三轮；报告成功率、失败/拒绝数与延迟/吞吐，不要求每个策略都正收益。

本次仅归档证据、核对数据与更新状态，没有修改应用代码，也没有把人工声明或少量 smoke 耗时转换成性能结论。
